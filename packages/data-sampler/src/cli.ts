#!/usr/bin/env node
/**
 * data-sampler command line entrypoint.
 *
 * Placeholder until qat-002 lands the generator. It exists so the scaffold
 * builds an executable `dist/cli.js` and CI has something to run.
 */

process.stdout.write("data-sampler: not yet implemented\n");
